#include "main.h"

/**
* _isupper - this code checks if upper letters
* @c: char check
* return: if uppercase return 1, else return 0
*/

int _isupper(int c)
{
	if (c >= 'A' && c <= 'Z')
		return (1);

	else

		return (0);
}
